version: '3.8'

services:
  api:
    container_name: api-prod
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "9037:9037"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - api-network

networks:
  api-network:
    driver: bridge 