version: '3.8'

services:
  api:
    container_name: api-prod
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - api-network

networks:
  api-network:
    driver: bridge 